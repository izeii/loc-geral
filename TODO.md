# TODO - Melhorias de UI/UX do site (GitHub Pages)

- [ ] Ajustar acessibilidade: adicionar estilos `:focus-visible` para `.barra`.
- [ ] Tornar cards responsivos: substituir width/height fixos por `min()` + `aspect-ratio` (mantendo estética).
- [ ] Enriquecer animação `reveal`: incluir `scale` e um glow sutil (sem quebrar performance).
- [ ] (Opcional) Remover/amenizar `<br><br><br>` usando margens/gaps no CSS.
- [ ] (Opcional) Habilitar stagger estável (delay incremental por índice) no JS/CSS.
- [ ] Testar visual: revisar em desktop e mobile e checar se header fixo não “corta” conteúdo.

